# Expo Continuous Native Generation
# android/ and ios/ are generated on demand via `npx expo prebuild`
# (or automatically by `npx expo run:android` / `expo run:ios`) and are
# never committed. Run prebuild locally if you need to inspect/edit them.
/android/
/ios/
.expo/
dist/
web-build/

# OSX
.DS_Store

# node.js
node_modules/
npm-debug.log
yarn-error.log

# Metro
.metro-health-check*

# Local env files
.env*.local

# Logs
*.log
